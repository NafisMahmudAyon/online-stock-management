import React from "react";

const CreateProduct = ({ shop }) => {
	return (
		<div>
			<h2>Create Product for {shop.name}</h2>
			{/* Product creation form and functionality */}
		</div>
	);
};

export default CreateProduct;
