import React from "react";

const AllProducts = ({ shop }) => {
	return (
		<div>
			<h2>All Products in {shop.name}</h2>
			{/* List all products with management functionality */}
		</div>
	);
};

export default AllProducts;
